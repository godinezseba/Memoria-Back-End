type User {
  id: ID!
  name: String!
  lastName: String
  email: String!
  firebaseId: String!
  isAdmin: Boolean
  companyType: String!
  companyId: ID!
  editableCompanies: [ID]
}

input UserInput {
  name: String!
  lastName: String
  email: String!
  companyType: String!
  companyId: ID!
  editableCompanies: [ID]
}
